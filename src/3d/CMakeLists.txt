add_subdirectory(ftn)
add_subdirectory(inter)
add_subdirectory(relax)
add_subdirectory(kernel)
add_subdirectory(cg)

set(3d_SOURCES
  ${ftn_SOURCES}
  ${inter_SOURCES}
  ${relax_SOURCES}
  ${kernel_SOURCES}
  ${cg_SOURCES}
  grid_func.cc
  grid_stencil.cc
  stencil_op.cc
  relax_stencil.cc
  residual.cc
  solver.cc
  PARENT_SCOPE
  )
